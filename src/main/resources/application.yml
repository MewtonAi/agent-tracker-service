micronaut:
  application:
    name: agent-tracker-service
  mcp:
    server:
      info:
        name: mcpdemo
        version: 0.0.1
      transport: HTTP
mongodb:
  uri: ${MONGODB_URI:`mongodb://localhost:27017/agent_tracker`}

idempotency:
  ttl-hours: ${IDEMPOTENCY_TTL_HOURS:72}
